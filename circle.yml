machine:
  python:
    version: 2.7
  environment:
    PYTHONPATH: ${HOME}

dependencies:
  pre:
    - pip install -U selenium
    - pip install webium
    - pip install --upgrade pip
   

test:
  override:
    - echo $PYTHONPATH
    - cd stratifi_tests && pytest