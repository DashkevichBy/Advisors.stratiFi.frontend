machine:
  python:
    version: 2.7
  environment:
    PYTHONPATH: ${HOME}

dependencies:
  pre:
    - pip install -U selenium

test:
  override:
    - echo $PYTHONPATH
    - python stratifi_tests/test_login.py