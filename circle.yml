machine:
  python:
    version: 2.7
  environment:
    PYTHONPATH: ${HOME}

dependencies:
  pre:
    - pip install -U selenium

test:
  override:
    - echo $PYTHONPATH
    - cd stratifi_tests && pytest test_login.py